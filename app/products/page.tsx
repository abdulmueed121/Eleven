import { ProductsPage } from '../site';
export const metadata = { title: 'Products', description: 'The ELEVEN products we build and operate.' };
export default function Page(){ return <ProductsPage/> }
