import { ContactPage } from '../site';
export const metadata = { title: 'Start a project', description: 'Tell ELEVEN about what you are building, rebuilding or scaling.' };
export default function Page(){ return <ContactPage/> }
